networks:
  comfycowboy_default:
    external: false

services:
  mariadb:
    profiles: [desktop]
  rabbitmq:
    profiles: [desktop]
  bot:
    profiles: [desktop]

  pipeline:
    build:
      context: .
      args:
        REQUIREMENTS: requirements.sparky.txt
    environment:
      - DATABASE_HOST=192.168.1.114
      - RABBITMQ_HOST=192.168.1.114
